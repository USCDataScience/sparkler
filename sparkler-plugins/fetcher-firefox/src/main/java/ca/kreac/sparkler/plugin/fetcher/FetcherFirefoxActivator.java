package ca.kreac.sparkler.plugin.fetcher;

import org.pf4j.Plugin;
import org.pf4j.PluginWrapper;

/**
 * Plugin Activator for start and stop call back
 */
public class FetcherFirefoxActivator extends Plugin {

    public FetcherFirefoxActivator(PluginWrapper wrapper) {
        super(wrapper);
    }
}
